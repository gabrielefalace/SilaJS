
describe ('matching object', function(){

    beforeEach(function () {
        /*
         instantiate / prepare what needed
         */
    });

   it('should be available', function(){
       var objectExists = !!matching;
       expect(objectExists).toBe(true);
   }) ;

});